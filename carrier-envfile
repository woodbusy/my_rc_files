rvm use system

if [ -z "$TKROOT" ] ; then
  export PATH=/opt/carrier/bin:$PATH
  export CARRIER_DEV=~/src/scanner-dev/carrier/src
  export TKROOT=$HOME/src/carrier/src

  alias irb="irb -I lib"
  alias devroot='cd $TKROOT' # shortcut to cd to the dev directory
fi
